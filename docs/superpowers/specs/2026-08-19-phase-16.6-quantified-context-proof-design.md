# Phase 16.6 — Quantified Context Proof: Design

## Problem

ForgeAI có hai "wow moment" đã được xác nhận:

1. **`try` output** — người dùng thấy ngay file nào sẽ được chọn mà không cần install
2. **Tiết kiệm token** — context nhỏ hơn đáng kể so với gửi cả repo

Vấn đề: cả hai moment này hiện **không có con số cụ thể**. `try` hiển thị `~42.1 KB selected (raw source)` nhưng không cho thấy 42 KB đó so với bao nhiêu và bao nhiêu phần trăm đã bị loại. `--compile-context` hiển thị `estimated tokens 4821/6000` nhưng không cho biết scope đầu vào.

Kết quả: người dùng không cảm nhận được giá trị thực sự; không có con số để share.

## Goal

Làm cho hai wow moment trở nên **có thể đo lường và share** bằng cách thêm:

1. **Exclusion metrics** vào `try` output — tỷ lệ indexed source bị loại khỏi context
2. **Source scope summary** vào `--compile-context` file-output mode — selected source / indexed source
3. **State-aware CTA** trong `try` — hướng dẫn next step dựa trên trạng thái thật của `.ai/`

## Metric Scope Clarification

**`try` và `--compile-context` đo file-level bytes, không phải excerpt bytes.**

- `selectedSourceBytes` = tổng kích thước file của các file được select (không phải excerpts)
- `totalSourceBytes` = tổng kích thước file của tất cả nodes trong dep graph
- Dep graph chỉ index extensions có parser (TS/JS/Python/Go), bỏ `.d.ts` và ignored dirs — do đó dùng **"indexed source"**, không dùng "repo"
- `estimated tokens` thể hiện kích thước ước tính của **toàn bộ compiled artifact** (selection metadata, rules, diagnostics, excerpts — không phải excerpts-only). Hai metrics bổ sung nhau nhưng đo hai thứ khác nhau.

Hai thứ này bổ sung nhau:
- `Selected ~42.1 KB of ~380.4 KB indexed source (89% excluded)` → cho biết scope đầu vào (file bytes)
- `estimated tokens 4821/6000` → cho biết kích thước artifact đã compile gửi lên model

## Non-Goals

- Provider-accurate token-savings percentage claims (Principle #5 cấm claim savings khi không có provider usage data; local estimation thì không bị cấm)
- Clipboard copy
- GitHub Action template
- `--report` cumulative savings (cần run record schema changes — defer)
- Phase 16.2c Rust parser (vẫn planned, ship độc lập)

---

## Feature 1: Exclusion Metrics trong `try`

### Output hiện tại

```
  Source size ~42.1 KB selected  (raw source — not compiled excerpts)
```

### Output sau phase này

```
  Selected    ~42.1 KB of ~380.4 KB indexed source  (89% excluded)
```

Dòng này hiển thị bất cứ khi nào `totalSourceBytes > 0`, kể cả khi selected === total (0% excluded). Ẩn chỉ khi `totalSourceBytes === 0` (không có indexed source files).

### Data model

Thêm field vào `TryReport`:

```typescript
export type TryReport = {
  objective: string;
  terms: string[];
  languages: Map<string, number>;
  totalFiles: number;
  selected: SelectedContextNode[];
  omittedCount: number;
  selectedSourceBytes: number;
  totalSourceBytes: number;           // NEW: sum of all dep graph node file sizes
};
```

### `buildTryReport` changes

```typescript
const totalSourceBytes = depGraph.nodes.reduce(
  (sum, node) => sum + fileSizer(node.path), 0
);
return { ..., totalSourceBytes };
```

`fileSizer` đã injectable — tests không cần đổi signature.

### `formatTryOutput` changes

Thay dòng source size:

```typescript
if (report.totalSourceBytes > 0) {
  const rawPct = Math.round((1 - report.selectedSourceBytes / report.totalSourceBytes) * 100);
  const pct = Math.max(0, Math.min(100, rawPct)); // clamp [0, 100]
  lines.push(
    `  Selected    ~${formatBytes(report.selectedSourceBytes)} of ~${formatBytes(report.totalSourceBytes)} indexed source  (${pct}% excluded)`
  );
}
```

Clamp percentage về `[0, 100]` để handle edge case fileSizer mock returns inflated sizes.

---

## Feature 2: Source Scope Summary trong `--compile-context` (file-output mode only)

### Scope constraint

`runCompileContext` (context-compiler.ts) returns early tại line 604–607 khi không có `--output`:

```typescript
if (!outputArg) {
  process.stdout.write(json);
  return;                     // ← console.log không bao giờ chạy sau đây
}
```

Source scope summary **chỉ xuất hiện trong file-output mode** (`--output <json>` được cung cấp). Behavior trong JSON stdout mode không thay đổi. Spec không support stderr alternative cho JSON stdout mode.

### Output sau phase này (file-output mode)

```
✓  compiled context JSON written to .ai/state/context/TASK-01.json
✓  compiled context Markdown written to .ai/state/context/TASK-01.md
✓  source scope   ~42.1 KB of ~380.4 KB indexed source  (89% excluded)
✓  estimated tokens 4821/6000
```

### Implementation

Trong `runCompileContext()`, sau khi write artifacts và trước `estimated tokens` log:

```typescript
const selectedPaths = new Set(artifact.selection.files.map((f) => f.path));
const selectedSourceBytes = dependencyGraph!.nodes
  .filter((n) => selectedPaths.has(n.path))
  .reduce((sum, n) => { try { return sum + fs.statSync(path.join(root, n.path)).size; } catch { return sum; } }, 0);
const totalSourceBytes = dependencyGraph!.nodes
  .reduce((sum, n) => { try { return sum + fs.statSync(path.join(root, n.path)).size; } catch { return sum; } }, 0);

if (totalSourceBytes > 0) {
  const pct = Math.max(0, Math.min(100, Math.round((1 - selectedSourceBytes / totalSourceBytes) * 100)));
  console.log(formatStatus('ok',
    `source scope  ~${formatBytes(selectedSourceBytes)} of ~${formatBytes(totalSourceBytes)} indexed source  (${pct}% excluded)`
  ));
}
console.log(formatStatus('ok', `estimated tokens ${artifact.budget.estimated_tokens}/${artifact.budget.limit_tokens}`));
```

`dependencyGraph` đã được read và validated trước line 602 — không cần re-read.

### `formatBytes` — shared utility

`formatBytes` hiện là private trong `try.ts`. Cần dùng ở cả `try.ts` và `context-compiler.ts` → extract vào `bin/lib/utils.ts`:

```typescript
export function formatBytes(n: number): string {
  if (n >= 1024 * 1024) return `${(n / (1024 * 1024)).toFixed(1)} MB`;
  if (n >= 1024) return `${(n / 1024).toFixed(1)} KB`;
  return `${n} bytes`;
}
```

`try.ts` xóa local copy và import từ `utils.js`.

---

## Feature 3: State-Aware CTA trong `try`

### Six states

| State | Condition | Recovery command |
|-------|-----------|-----------------|
| `uninitialized` | `graph.json` không tồn tại | `--profile auto` |
| `needs-reinit` | `graph.json` malformed + manifest valid | `mktemp` backup + `--upgrade` (bare, giữ profile) |
| `needs-reinit-auto` | `graph.json` malformed + manifest invalid/missing | `mktemp` backup + `--upgrade --profile auto` |
| `needs-graph` | `graph.json` valid, dep graph file không tồn tại | `--refresh-codegraph` |
| `needs-refresh` | Cả hai file tồn tại nhưng `checkDependencyGraphHealth` non-ok | `--refresh-codegraph` |
| `ready` | `graph.json` valid, dep graph health `ok` | `--compile-context` |

**Tại sao hai nhánh `needs-reinit`:**
- Bare `--upgrade` (không explicit profile): `init.ts:363` dùng `manifestProfile` → giữ nguyên profile → manifest/templates không thay đổi
- `--upgrade --profile auto`: `init.ts:363` dùng `'auto'` → `detectProjectProfile()` → có thể ra profile khác → `writeManifest(new_profile)` — thay đổi manifest không cần thiết khi graph repair
- Bare `--upgrade` fail khi manifest invalid (init.ts:348) → cần `--profile auto` trong nhánh đó

**Backup uniqueness — dùng `mktemp` (không dùng timestamp):**
```bash
mv .ai/codegraph/graph.json "$(mktemp .ai/codegraph/graph.json.backup.XXXXXX)"
```
`mktemp` tạo file unique atomically — không có collision dù gọi nhiều lần trong cùng giây. Cần `.ai/codegraph/` tồn tại (đã tồn tại nếu `graph.json` tồn tại).

`--refresh-codegraph` không fix được — nó chỉ write `dependency-graph.json`, không đụng đến `graph.json`.

**Schema-invalid-but-parseable graphs không cần riêng state `needs-reinit`**: Cả `readCuratedCodeGraph` và `tryReadCuratedCodeGraph` đều dùng `parseCuratedCodeGraph` + `normalizeCuratedGraph`, nên `{"nodes": [null], ...}` được normalize an toàn — không crash, CTA `ready` là đúng.

### `CtaState` type

```typescript
export type CtaState = 'uninitialized' | 'needs-reinit' | 'needs-reinit-auto' | 'needs-graph' | 'needs-refresh' | 'ready';
```

### `detectCtaState` function

Internal, không export (chỉ `runTry()` gọi):

```typescript
import { DEPENDENCY_GRAPH_PATH, readDependencyGraph, checkDependencyGraphHealth } from './dependency-graph.js';

function detectCtaState(projectRoot: string): CtaState {
  const curatedGraphFile = path.join(projectRoot, '.ai', 'codegraph', 'graph.json');
  const depGraphFile = path.join(projectRoot, DEPENDENCY_GRAPH_PATH);

  // 1. Curated graph missing → not initialized
  if (!fs.existsSync(curatedGraphFile)) return 'uninitialized';

  // 2. Curated graph exists but JSON parse fails → needs-reinit or needs-reinit-auto
  // Schema-invalid-but-parseable graphs are handled by normalizeCuratedGraph at
  // the readCuratedCodeGraph layer — detectCtaState only catches parse failures.
  try {
    JSON.parse(fs.readFileSync(curatedGraphFile, 'utf-8'));
  } catch {
    // Check manifest to pick correct recovery command:
    // bare --upgrade only works when manifest profile is valid (init.ts:348, :363).
    const manifestPath = path.join(projectRoot, '.ai', 'manifest.json');
    if (fs.existsSync(manifestPath)) {
      try {
        const m = JSON.parse(fs.readFileSync(manifestPath, 'utf-8')) as Record<string, unknown>;
        if (m !== null && typeof m === 'object' && !Array.isArray(m) && typeof m['profile'] === 'string') {
          return 'needs-reinit';           // manifest valid → bare --upgrade safe, profile preserved
        }
      } catch { /* fall through */ }
    }
    return 'needs-reinit-auto';            // manifest invalid/missing → need --profile auto
  }

  // 3. Dep graph file missing → needs codegraph build
  if (!fs.existsSync(depGraphFile)) return 'needs-graph';

  // 4. Dep graph exists but stale/invalid
  const depGraph = readDependencyGraph(projectRoot);
  const health = checkDependencyGraphHealth(projectRoot, depGraph);
  if (health.status !== 'ok') return 'needs-refresh';

  return 'ready';
}
```

Ghi chú: dùng raw `JSON.parse` thay vì `readCuratedCodeGraph`/`readManifestResult` để tránh side effects (stderr logging, exitCode). Manifest check là inline, lightweight, không import thêm module.

### `formatTryOutput` signature change

Backward-compatible (default `'uninitialized'`):

```typescript
export function formatTryOutput(report: TryReport, ctaState: CtaState = 'uninitialized'): string
```

### CTA variants

**`uninitialized`** — current behavior (unchanged):
```
  Ready to use ForgeAI on this repository?
    npx forgeai-agentic-init@latest --profile auto
    npx forgeai-agentic-init@latest --refresh-codegraph
    npx forgeai-agentic-init@latest --compile-context --objective "<your objective>"
```

**`needs-reinit`** (manifest valid — profile preserved):
```
  graph.json is corrupted. To recover (backs up your current file):
    mv .ai/codegraph/graph.json "$(mktemp .ai/codegraph/graph.json.backup.XXXXXX)"
    npx forgeai-agentic-init@latest --upgrade
```

**`needs-reinit-auto`** (manifest invalid/missing — needs profile re-detection):
```
  graph.json is corrupted. To recover (backs up your current file):
    mv .ai/codegraph/graph.json "$(mktemp .ai/codegraph/graph.json.backup.XXXXXX)"
    npx forgeai-agentic-init@latest --upgrade --profile auto
```

**`needs-graph`**:
```
  ForgeAI is initialized. Build the codegraph to continue:
    npx forgeai-agentic-init@latest --refresh-codegraph
```

**`needs-refresh`**:
```
  Codegraph is stale or invalid. Refresh it:
    npx forgeai-agentic-init@latest --refresh-codegraph
```

**`ready`**:
```
  ForgeAI is ready. Compile context for your objective:
    npx forgeai-agentic-init@latest --compile-context --objective "<your objective>"
```

Objective placeholder `<your objective>` không thay bằng objective thật — user copy-paste safety.

---

## Shared Normalizer Architecture

`normalizeCuratedGraph` hiện là private trong `try.ts`. Phase này move nó sang `context-pack.ts` (cùng module với `readCuratedCodeGraph`) và dùng nó ở 3 chỗ:

### Move `normalizeCuratedGraph` → `context-pack.ts`

Export từ `context-pack.ts` thay vì `try.ts`. `try.ts` import lại từ `context-pack.js`.

### Private `parseCuratedCodeGraph` shared bởi cả hai readers

`tryReadCuratedCodeGraph` (context-pack.ts:415–423) có đường read riêng, raw cast — không phải wrapper của `readCuratedCodeGraph`. Cả hai cần được refactor để dùng chung một private function với **discriminated return type** (để `readCuratedCodeGraph` giữ được error detail):

```typescript
type ParseCuratedResult =
  | { ok: true; graph: CodeGraph }
  | { ok: false; error: unknown };

// Private — shared by both exported readers
function parseCuratedCodeGraph(graphPath: string): ParseCuratedResult {
  try {
    return { ok: true, graph: normalizeCuratedGraph(JSON.parse(fs.readFileSync(graphPath, 'utf8'))) };
  } catch (error) {
    return { ok: false, error };  // JSON.parse or fs error; normalizeCuratedGraph never throws
  }
}

export function readCuratedCodeGraph(repositoryRoot = root): CodeGraph | null {
  const graphPath = path.join(repositoryRoot, '.ai', 'codegraph', 'graph.json');
  if (!fs.existsSync(graphPath)) {
    console.error('Error: .ai/codegraph/graph.json not found. Run forgeai-init first.');
    process.exitCode = 1;
    return null;
  }
  const result = parseCuratedCodeGraph(graphPath);
  if (!result.ok) {
    console.error(`Error: .ai/codegraph/graph.json is invalid (${getErrorMessage(result.error)}).`);
    process.exitCode = 1;
    return null;
  }
  return result.graph;
}

export function tryReadCuratedCodeGraph(repositoryRoot: string): CodeGraph | null {
  const graphPath = path.join(repositoryRoot, '.ai', 'codegraph', 'graph.json');
  if (!fs.existsSync(graphPath)) return null;
  const result = parseCuratedCodeGraph(graphPath);
  return result.ok ? result.graph : null;
}
```

`ParseCuratedResult` là private type (không export). `parseCuratedCodeGraph` trả `{ ok: false, error }` chỉ khi JSON.parse hoặc fs.readFileSync throw. `normalizeCuratedGraph` không bao giờ throw — nên `ok: true` là đảm bảo khi parse thành công.

### Tại sao `detectCtaState` chỉ cần check JSON.parse

Sau khi cả hai readers dùng `parseCuratedCodeGraph` + `normalizeCuratedGraph`, schema-invalid-but-parseable graphs được normalize an toàn. `detectCtaState` chỉ cần: "parse được không?" — nếu không → `needs-reinit`.

### Callers sau khi thay đổi

| Caller | Trước | Sau |
|--------|-------|-----|
| `readCuratedCodeGraph` | raw JSON.parse cast | `parseCuratedCodeGraph` (normalize bên trong) |
| `tryReadCuratedCodeGraph` | raw JSON.parse cast | `parseCuratedCodeGraph` (normalize bên trong) |
| `runTry()` in `try.ts` | `normalizeCuratedGraph(tryReadCuratedCodeGraph(...))` | `tryReadCuratedCodeGraph(...)` (đã normalized) |
| `detectCtaState` | JSON.parse + schema check | JSON.parse only |

---

## README Update

### Section "Zero-Setup Preview"

Update example output với format mới và "indexed source" wording:

```
ForgeAI context proof — "add user authentication"

  Detected    TypeScript · Go  (43 source files)
  Relevant    8 files matched
  Selected    ~42.1 KB of ~380.4 KB indexed source  (89% excluded)

  Included files:
  ────────────────────────────────────────────────────────────────────────────
    src/auth/middleware.ts    seed: source path match (score 6)
    src/auth/session.ts       dependency of src/auth/middleware.ts
    ...

  Ready to use ForgeAI on this repository?
    npx forgeai-agentic-init@latest --profile auto
    npx forgeai-agentic-init@latest --refresh-codegraph
    npx forgeai-agentic-init@latest --compile-context --objective "<your objective>"
```

### New subsection: "After initialization"

Show compile-context output snippet để người dùng thấy wow moment thứ 2 trước khi install:

```markdown
After initialization, `--compile-context --output <path>` shows exactly what was scoped:

```
✓  source scope   ~42.1 KB of ~380.4 KB indexed source  (89% excluded)
✓  estimated tokens 4821/6000
```
```

---

## File Map

| Action | Path | What changes |
|--------|------|--------------|
| Modify | `bin/lib/utils.ts` | Add `formatBytes` export |
| Modify | `bin/lib/context-pack.ts` | Move `normalizeCuratedGraph` here (export); add private `parseCuratedCodeGraph`; both `readCuratedCodeGraph` and `tryReadCuratedCodeGraph` use `parseCuratedCodeGraph` |
| Modify | `bin/lib/try.ts` | Add `totalSourceBytes` to `TryReport`; update `buildTryReport`; remove local `formatBytes`; remove local `normalizeCuratedGraph` (import from context-pack); add `CtaState` type; update `formatTryOutput` signature and body; add `detectCtaState`; update `runTry` |
| Modify | `bin/lib/context-compiler.ts` | Import `formatBytes` from utils; add source scope log in `runCompileContext` (file-output mode only) |
| Modify | `test/try.test.ts` | New tests for `totalSourceBytes`, exclusion %, `CtaState` variants, 0%-excluded case; update existing source-size string assertions |
| Modify | `test/dist.test.ts` | Add E2E: `try` output contains `% excluded` string; `try` CTA variants for partial `.ai/` states; `--compile-context --output` output contains `source scope` |
| Modify | `CHANGELOG.md` | Add `## 3.15.0` entry |
| Modify | `ROADMAP.md` | Add Phase 16.6 under Phase 16 block; Phase 17 (session locking) không đổi |
| Modify | `README.md` | Update Zero-Setup Preview example + add compile-context snippet |
| Modify | `package.json` + `package-lock.json` | Version `3.14.0` → `3.15.0` |

---

## Testing

### Unit tests (try.test.ts)

**`buildTryReport`:**
- `totalSourceBytes === 0` khi dep graph rỗng
- `totalSourceBytes` = sum của tất cả node sizes qua `fileSizer` (không chỉ selected nodes)
- `totalSourceBytes >= selectedSourceBytes` (selected là subset)

**`formatTryOutput` — exclusion metric:**
- Hiển thị dòng `of ... indexed source (89% excluded)` khi `totalSourceBytes > 0`
- **Không ẩn** khi `selectedSourceBytes === totalSourceBytes` — hiển thị `(0% excluded)`
- Không hiển thị khi `totalSourceBytes === 0`
- Percentage clamped [0, 100] khi fileSizer trả về inflated size

**`formatTryOutput` — CTA states:**
- `ctaState = 'uninitialized'` (default): chứa cả 3 commands (profile, refresh, compile)
- `ctaState = 'needs-reinit'`: chứa `mktemp` backup command và bare `--upgrade`; chứa "corrupted"; không chứa `--profile auto`; không chứa `--refresh-codegraph`
- `ctaState = 'needs-reinit-auto'`: chứa `mktemp` backup command và `--upgrade --profile auto`; chứa "corrupted"; không chứa `--refresh-codegraph`
- `ctaState = 'needs-graph'`: chứa `--refresh-codegraph`, không chứa `--profile auto`
- `ctaState = 'needs-refresh'`: chứa `--refresh-codegraph` và "stale or invalid" message
- `ctaState = 'ready'`: chứa `--compile-context`, không chứa `--refresh-codegraph`

### E2E tests (dist.test.ts)

**`try` output:**
- Repo với source files: output chứa `% excluded`
- Repo không có source files: output không chứa dòng `Selected ... of`

**`try` CTA state detection (filesystem setup per test):**
- Không có `graph.json`: CTA chứa `--profile auto` (uninitialized)
- `graph.json` malformed + manifest valid: state `needs-reinit`; CTA chứa `mktemp` backup và bare `--upgrade`; không chứa `--profile auto`
- `graph.json` malformed + manifest invalid: state `needs-reinit-auto`; CTA chứa `mktemp` backup và `--upgrade --profile auto`
- `graph.json` malformed + manifest missing: state `needs-reinit-auto`; CTA chứa `--upgrade --profile auto`
- `graph.json` schema-invalid (`{"nodes": [null]}`): CTA ra `ready` (normalized bởi `parseCuratedCodeGraph`); E2E xác nhận compile sau đó không crash
- Recovery E2E (manifest valid): setup malformed `graph.json` → run `try` → execute `mktemp` backup + `--upgrade` → verify `graph.json` recreated valid JSON, manifest profile unchanged → run `try` lại → CTA `ready`
- Recovery E2E (manifest invalid): same graph setup + corrupt `manifest.json` → verify `--upgrade --profile auto` succeeds
- Backup uniqueness E2E: chạy `mktemp` command hai lần → verify hai backup files tồn tại với tên khác nhau (mktemp đảm bảo uniqueness atomically)
- `graph.json` valid, dep graph không có: CTA chứa `--refresh-codegraph`, chứa "Build the codegraph"
- `graph.json` valid, dep graph malformed JSON: CTA chứa `--refresh-codegraph`, chứa "stale or invalid"
- `graph.json` valid, dep graph valid: CTA chứa `--compile-context`

**`--compile-context` (file-output mode):**
- Với `--output`: output chứa `source scope`
- Không có `--output` (JSON stdout): output là pure JSON, không chứa `source scope`

---

## Explicit Constraints

- **Không dùng "token savings"** — chỉ dùng "excluded" (bytes). Product Principle #5.
- `formatTryOutput` vẫn là pure function — `detectCtaState` chỉ trong `runTry()`.
- `totalSourceBytes` được tính qua `fileSizer` injectable — unit tests không hit filesystem.
- `formatTryOutput(report)` backward-compatible (default `ctaState = 'uninitialized'`).
- Source scope log trong `--compile-context`: file-output mode only. JSON stdout mode không thay đổi.
- `detectCtaState` không export — internal to `try.ts`.

---

## Version

`3.14.0` → `3.15.0` (via `npm version 3.15.0 --no-git-tag-version`)

ROADMAP: Phase 16.6 thêm vào sau Phase 16.5 block. Phase 17 (Concurrent session locking) **không đổi số**.

Phase 16.2c (Rust parser) vẫn planned — ship độc lập, không block phase này.
